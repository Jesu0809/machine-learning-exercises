services:
  - type: web
    name: ml-frontend-flask
    env: python
    buildCommand: "cd ml-frontend && npm install && npm run build && cd .. && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
